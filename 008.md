def great(name):

    `name` is the parameter (variable)
    
    The value of `name` is the argument


list = [A, B, C]

list.index("A") #1

new_text = ''.join(new_text_list)  # 用空字符串连接列表元素

    



